import React from 'react'
import ReactDOM from 'react-dom/client'
import GameLayout from './game.tsx'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <GameLayout />
  </React.StrictMode>,
)
